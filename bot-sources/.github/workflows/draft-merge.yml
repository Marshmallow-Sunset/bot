name: проверка PR в draft
on:
  pull_request:
    types: [opened, edited]
    branches:
      - draft-*
jobs:
  main-job:
    runs-on: ubuntu-latest
    steps:
      - name: main
        run: |

          TARGET="${{ github.base_ref }}"
          FROM="${{ github.head_ref }}"

          if [[ "$FROM" == ocode-* ]] && [[ "$TARGET" == draft-* ]]; then
            echo "Слияние может быть одобрено"
            exit 0

          elif [[ "$FROM" != ocode-* ]] && [[ "$TARGET" == draft-* ]]; then
            echo "Слияние запрещено. FROM != ocode"
            exit 1
          fi
